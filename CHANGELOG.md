# Changelog

このプロジェクトの主な変更はこのファイルに記録します。

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に従います。

## [Unreleased]

### Security

- push前監査で検出された既知の依存脆弱性を解消するため、安全版へ依存関係とロックファイルを更新した。
- Sakura AIのアカウントトークンがソースコード、ブラウザ、Git、ログへ残らないように、Windows DPAPIで暗号化してユーザー領域へ保存し、サーバー起動時だけ読み込む仕組みを追加した。
- 長いSakura AIトークンを安全かつ簡単に登録できるように、クリップボードから形式検証付きで読み取り、直後にクリップボードを消去する登録方法を追加した。

### Changed

- 作業開始時の共通指針見落としを防ぐため、調査やコマンド実行より前に `COMMON-AGENTS.md` を先頭から末尾まで読み、EOFを確認する必須ゲートを追加した。
- ログオン時の自動起動でも更新後のビルドを確実に実行できるように、Bunの実行ファイルをユーザー領域から明示的に解決するよう変更した。
- 数値変換タブの既定状態でも「400おくどる」などの自然な通貨表現を使えるように、一般表記はローカルで解析し、曖昧な表現だけSakura AIで補完する通貨変換経路へ統一した。

### Fixed

- 「せんえん」「千円」など数値を省略した単位表現がAI解釈へ回って桁違いの換算結果になることを防ぐため、省略された係数を1としてローカルで確定的に解釈するよう修正した。
- 日本語の金額を個別例に依存せず正確に換算できるように、漢数字・ひらがな・カタカナ・大字・算用数字が混在する数詞を桁構造としてローカル解析するよう修正した。
- LLMによる値と単位の二重適用を防ぐため、Sakura AIには単位展開済みの絶対額・正規化結果・換算結果を返させ、アプリ側の独立計算およびローカル解析と一致した結果だけを採用するよう修正した。

### Added

- CodexリセットAPIから取得したリセットクレジットのexpire dateをリアルタイム表示するタブを追加。
- 全角・半角、空白、自然文による表示指定を受け付けられるように、Sakura AIで入力意図を解析し、検証済みの条件を用いてアプリ側で確定的に計算するAIパラメーター単位変換を追加した。
